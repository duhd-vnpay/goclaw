-- Migration 000086 was harness_workflow_runs (Ardenn workflow).
-- DROPPED in v3.15.0-beta.27 merge (DROP Ardenn engine decision).
-- This is a no-op stub to preserve sequential migration order for fresh installs.
-- Existing production DBs have version >= 92 → never re-applied.
SELECT 1;
